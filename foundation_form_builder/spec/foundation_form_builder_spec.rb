require 'spec_helper'

describe FoundationFormBuilder do
  it 'has a version number' do
    expect(FoundationFormBuilder::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
