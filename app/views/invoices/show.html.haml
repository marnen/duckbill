.user
  %p= _ 'From'
  = render 'address', person: @user

.client
  %p= _ 'Client'
  = render 'address', person: @client

.project= _ 'Project: %{project}' % {project: @project.name}

%table.time_entries
  %thead
    %tr
      %th= _ 'Date'
      %th= _ 'Description'
      %th= _ 'Hours'
      %th= _ 'Notes'
  %tbody
    - @time_entries.each do |time_entry|
      %tr
        %td.date= time_entry.date
        %td.description= time_entry.description
        %td.hours= time_entry.hours
        %td.notes= time_entry.notes
